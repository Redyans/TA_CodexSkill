# Shader 变体、功能支持与低配风险报告

> **适用触发条件**：新生成或重写 Shader，或新增、启用、删除会影响变体的 `#define`、`shader_feature`、`multi_compile`、`include_with_pragmas`。
>
> **重要说明**：本文的变体数量下限和上限均为参考值，**不等于 Unity 最终编译、剥离或打包数量**。项目自定义宏管理、ShaderVariantCollection、平台 stripping、材质 Keyword 状态、RendererFeature 和实际构建设置都会改变最终数量。

## 1. 变更范围

| 项目 | 内容 |
| --- | --- |
| Shader / 路径 | `<Shader 名称与项目路径>` |
| 变更类型 | `<新建 / 重写 / 新增或调整 pragma / 宏变更>` |
| 目标平台与图形 API | `<例如 Android GLES3 / Vulkan；未知必须写未知>` |
| Shader Model | `<逐 Pass 填写 target；说明高于 2.0 的技术原因>` |
| 受影响材质、RendererFeature、动态加载路径 | `<资产、脚本或未确认项>` |
| 兼容性基线 | `<新 Shader / 既有 Shader 的冻结版本或 A/B 基线>` |

## 2. 变体参考估算

逐 Pass 填写；`include_with_pragmas` 引入的 pragma 必须单独列出来源。固定 `#define` 只有在改变 pragma 选择或间接引入变体时才计入。

| Pass / LightMode | 变体来源与阶段 | 作用域 / 互斥组 | 下限参考 | 上限参考 | 证据或未知项 |
| --- | --- | --- | ---: | ---: | --- |
| `<Forward>` | `<shader_feature_local_fragment _A _B>` | `<local / 互斥或独立>` | `<数值>` | `<数值>` | `<材质、pragma 或 include 路径>` |
| `<其他 Pass>` | `<...>` | `<...>` | `<数值>` | `<数值>` | `<...>` |
| **Shader 合计** | **按 Pass 分别求和** | **不要跨 Pass 直接相乘** | **`<数值>`** | **`<数值>`** | `<计算前提>` |

计算口径：

- **下限参考**：按已确认材质 Keyword 状态、固定宏和不可剥离组合保守展开；无法确定的收集/剥离路径标为“未知”，不以猜测填零。
- **上限参考**：按当前可见的所有启用 pragma 选项组（含 `include_with_pragmas`）作理论笛卡尔积；互斥组按实际组内选项数计算。
- **最终数量**：必须与实际 Variant Capture、Unity 构建日志或包体检查分开记录。理论 pragma 组合不能替代实际剥离验证。

## 3. 引擎通用功能支持状态

状态只可填写：`支持`、`默认未启用`、`默认不支持`、`不适用`。默认注释的 pragma、Pass 或宏必须填“默认未启用”或“默认不支持”，不能仅因源码留有注释而标为支持。

| 功能 | 状态 | Pass / Keyword / 配置依据 | 验证或限制 |
| --- | --- | --- | --- |
| 阴影（接收 / 投射） | `<状态>` | `<依据>` | `<限制>` |
| DepthOnly | `<状态>` | `<依据>` | `<限制>` |
| DepthNormals | `<状态>` | `<依据>` | `<限制>` |
| Meta / 烘焙 | `<状态>` | `<依据>` | `<限制>` |
| SSAO | `<状态>` | `<依据>` | `<限制>` |
| Light Cookies | `<状态>` | `<依据>` | `<限制>` |
| Forward+ | `<状态>` | `<依据>` | `<限制>` |
| Detail | `<状态>` | `<依据>` | `<限制>` |
| LOD Cross Fade | `<状态>` | `<依据>` | `<限制>` |
| DOTS | `<状态>` | `<依据>` | `<限制>` |
| GPU Instancing | `<状态>` | `<依据>` | `<限制>` |
| Fog | `<状态>` | `<依据>` | `<限制>` |
| Additional Lights | `<状态>` | `<依据>` | `<限制>` |
| 透明 / BlendMode | `<状态>` | `<依据>` | `<限制>` |

## 4. 移动端与低配风险

| 风险项 | 影响 | 缓解或验证结果 |
| --- | --- | --- |
| Shader Model / 图形 API 门槛 | `<高 target 对低端设备和 API 的兼容性影响>` | `<验证设备或未验证项>` |
| 变体数量与编译/内存 | `<构建时间、包体、运行时加载风险>` | `<stripping / SVC / Capture 结果>` |
| 纹理采样与 ALU | `<高屏幕覆盖、法线/高光/分支成本>` | `<Profiler 或设备测量>` |
| 透明 Overdraw 与额外 Pass | `<排序、深度预写、描边、阴影或全屏 Pass>` | `<Frame Debugger / GPU 数据>` |
| 临时 RT 与屏幕资源 | `<带宽、MSAA、动态分辨率、多相机风险>` | `<RendererFeature 验证>` |
| 运行时 Keyword 切换 | `<变体缺失、卡顿或动态加载风险>` | `<材质/动态路径验证>` |

## 5. 实际验证与未验证项

- **已完成**：`<Unity 导入、Shader 编译、目标材质、Frame Debugger、真机、构建日志或 Variant Capture>`
- **实际编译/保留变体证据**：`<日志、Capture、构建产物路径或结论>`
- **未完成与剩余风险**：`<明确不能由静态检查证明的内容>`
- **结论**：`<是否可交付；不确定时说明阻塞或限制>`
